stages:
  - test

# Data quality tests (import-tests and other data validation)
test_data_quality:
  stage: test
  image: python:3.12
  before_script:
    - pip install --upgrade pip
    - pip install -e .[dev]
  script:
    - echo "Running data quality and import tests..."
    - pytest tests/import-tests/ -v